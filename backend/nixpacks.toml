[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["npm run prisma:generate", "npx prisma migrate deploy"]

[start]
cmd = "npm run start:prod"
